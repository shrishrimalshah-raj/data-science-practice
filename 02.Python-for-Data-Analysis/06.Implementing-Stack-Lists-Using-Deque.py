# importing deque
from collections import deque

# initializing
numbers = deque()

# Adding items to stack
numbers.append(26)
numbers.append(54)
numbers.append(12)
numbers.append(47)
numbers.append(76)

# Removing last element from stack
numbers.pop()
numbers.pop()
